# InstantDB App ID (get from instantdb.com dashboard)
VITE_INSTANTDB_APP_ID=your_instantdb_app_id_here

# Resend API Key - DO NOT add here with VITE_ prefix
# This should be set in Netlify dashboard as RESEND_API_KEY
# for security reasons (server-side only, never exposed to client)
